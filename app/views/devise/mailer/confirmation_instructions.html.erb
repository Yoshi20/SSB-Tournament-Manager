<p><%= t('devise.mailer.welcome', locale: @resource.country_code) %> <%= @email %></p>

<p><%= t('devise.mailer.confirm', locale: @resource.country_code) %></p>

<p><%= link_to t('devise.mailer.confirm_account', locale: @resource.country_code), confirmation_url(@resource, confirmation_token: @token) %></p>
