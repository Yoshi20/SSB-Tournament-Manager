- flash.each do |key, value|
  - if value.present?
    .alert.alert-dismissable{class: "#{flash_class(key)} fade in"}
      = button_tag "&times;".html_safe, class: "close", data: {dismiss: "alert"}, aria: {hidden: true}
      = value
