<%= @tournament.name %>
===============================================

<%= t('hi', locale: @locale) %> <%= @user.username %>.

<%= t('tournaments.mailer.too_few_registrations', tournament: @tournament.name.gsub('(cancelled) ', ''), locale: @locale).html_safe %>

<%= t('tournaments.mailer.date', locale: @locale) %> <%= @tournament.date.to_s(:custom_datetime_with_weekday) %>

<%= t('tournaments.mailer.to_join_an_other2', locale: @locale) %>
  <%= @url %>

<%= t('tournaments.mailer.we_hope_to_see_you', locale: @locale) %>
