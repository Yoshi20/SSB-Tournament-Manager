<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1><%= @tournament.name %></h1>
    <p>
      <%= t('hi', locale: @locale) %> <%= @user.username %>.
    </p>
    <p>
      <%= t('tournaments.mailer.player_left_tournament', locale: @locale) %>
    </p>
    <p>
      <%= t('tournaments.mailer.to_check_out_registration', link: link_to(t('tournaments.mailer.this_link', locale: @locale), @url), locale: @locale).html_safe %>
    </p>
    <p><%= t('tournaments.mailer.we_wish_you_good_luck', locale: @locale) %></p>
  </body>
</html>
