<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1><%= @tournament.name %></h1>
    <p>
      <%= t('tournaments.mailer.hi') %> <%= @user.username %>.
    </p>
    <p>
      <%= t('tournaments.mailer.too_few_registrations', tournament: @tournament.name.gsub('(cancelled) ', '')).html_safe %>
    </p>
    <p>
      <strong><%= t('tournaments.mailer.date') %></strong> <del><%= @tournament.date.to_s(:custom_datetime_with_weekday) %></del><br>
    </p>
    <p>
      <%= t('tournaments.mailer.to_join_an_other', link: link_to(t('tournaments.mailer.this_link'), @url)).html_safe %>
    </p>
    <p><%= t('tournaments.mailer.we_hope_to_see_you') %></p>
  </body>
</html>
