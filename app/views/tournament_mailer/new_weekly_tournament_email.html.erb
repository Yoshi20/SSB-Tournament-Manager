<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1><%= @tournament.name %></h1>
    <p>
      <%= t('hi', locale: @locale) %> <%= @user.username %>.
    </p>
    <p>
      <%= t("tournaments.mailer.new_weekly_#{@tournament.country_code}", locale: @locale) %>
    </p>
    <p>
      <strong><%= t('tournaments.mailer.name', locale: @locale) %></strong> <%= @tournament.name %><br>
      <strong><%= t('tournaments.mailer.date', locale: @locale) %></strong> <%= @tournament.date.to_s(:custom_datetime_with_weekday) %><br>
      <strong><%= t('tournaments.mailer.fee', locale: @locale) %></strong> <%= @tournament.registration_fee %> CHF<br>
      <strong><%= t('tournaments.mailer.location', locale: @locale) %></strong><br>
      <%- @tournament.location.split(',').each do |str| %>
        &emsp;<%= str.strip %><br>
      <%- end %>
    </p>
    <p>
      <%= t('tournaments.mailer.to_join', link: link_to(t('tournaments.mailer.this_link', locale: @locale), @url), locale: @locale).html_safe %>
    </p>
    <p>
      <%= t('tournaments.mailer.we_wish_you_good_luck', locale: @locale) %>
    </p>
    <p>
      <%= t('tournaments.mailer.if_you_dont_want', locale: @locale) %>
    </p>
  </body>
</html>
