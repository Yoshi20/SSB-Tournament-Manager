<%= t("users.mailer.welcome_#{@user.country_code}", locale: @locale) %>
===============================================

<%= t('users.mailer.signed_up', locale: @locale) %> <%= @user.username %>.

<%= t('users.mailer.to_join2', locale: @locale) %>
  <%= @url %>.

<%= t('users.mailer.thanks', locale: @locale) %>
