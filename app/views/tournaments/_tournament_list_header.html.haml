%tr
  %th.d-none.d-md-table-cell
  %th{style: 'min-width: 5rem'}= t('tournaments.date')
  %th= t('tournaments.name')
  %th.text-center.d-sm-none= t('tournaments.city')
  %th.d-none.d-sm-table-cell= t('tournaments.location')
  %th.text-center.d-none.d-md-table-cell= t('tournaments.seats')
